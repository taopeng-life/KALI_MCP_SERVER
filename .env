# 身份验证（可选；留空表示不启用鉴权）
API_KEY=your-secret-key

# 超时策略
# 0 表示不限时；IGNORE_TIMEOUT=1 则强制忽略客户端传入的 timeout
DEFAULT_TIMEOUT=0
IGNORE_TIMEOUT=1

# 可选调优
SLOW_THRESHOLD_SEC=3.0    # 慢日志阈值（秒）
JOB_MAX_LINES=5000        # 每个作业保留的最大日志行数（内存 ring buffer）
JOB_TTL_SEC=3600          # 作业完成后在内存中保留的时间（秒）
